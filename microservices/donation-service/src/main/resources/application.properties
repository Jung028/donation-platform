# Server configuration
server.port=8082
# Server configuration
server.port=8082
spring.application.name=donation-service

# Database configuration
spring.datasource.url=jdbc:h2:mem:donationdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Server configuration
server.port=8082
spring.application.name=donation-service

# Database configuration
spring.datasource.url=jdbc:h2:mem:donationdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# External service URLs
app.services.account-service.url=http://account-service:8081
app.services.transaction-service.url=http://transaction-service:8083

# Logging
logging.level.com.charity.donationservice=INFO
logging.level.org.springframework.web=INFO

# Feign client configuration
feign.client.config.default.connectTimeout=5000
feign.client.config.default.readTimeout=5000
feign.client.config.default.loggerLevel=basic
# External service URLs
app.services.account-service.url=http://account-service:8081
app.services.transaction-service.url=http://transaction-service:8083

# Docker profile
spring.config.activate.on-profile=docker
# You can override any properties for docker environment here

# Logging
logging.level.com.charity.donationservice=INFO
logging.level.org.springframework.web=INFO

# Feign client configuration
feign.client.config.default.connectTimeout=5000
feign.client.config.default.readTimeout=5000
feign.client.config.default.loggerLevel=basic
# Application name
spring.application.name=donation-service

# Database configuration
spring.datasource.url=jdbc:h2:mem:donationdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
